{
  "name": "mi-health-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency Xiaomi Fitness relatives MCP server for Cloudflare Workers",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
